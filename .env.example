# ===========================================
# URL Shortener Microservices - Environment Variables
# ===========================================
# Copy this file to .env and modify as needed
# Usage: docker compose --env-file .env up -d

# ===========================================
# Storage Configuration
# ===========================================

# Storage backend: "memory" or "redis"
# - memory: In-memory storage (data lost on restart)
# - redis: Redis storage (persistent data)
STORAGE_TYPE=memory

# Redis server address (only used when STORAGE_TYPE=redis)
REDIS_URL=redis:6379

# ===========================================
# Service Ports
# ===========================================

# Frontend port (default: 3000)
FRONTEND_PORT=3000

# URL Service port (default: 8080)
URL_SERVICE_PORT=8080

# Analytics Service port (default: 8081)
ANALYTICS_SERVICE_PORT=8081

# Redis port (default: 6379)
REDIS_PORT=6379

# ===========================================
# Frontend Configuration
# ===========================================

# URL Service endpoint (for browser requests)
NEXT_PUBLIC_URL_SERVICE=http://localhost:8080

# Analytics Service endpoint (for browser requests)
NEXT_PUBLIC_ANALYTICS_SERVICE=http://localhost:8081

# Short URL domain (displayed to users for shortened links)
# Separate from API for cleaner, more memorable URLs
PUBLIC_SHORT_URL_DOMAIN=http://s.example.local

# ===========================================
# Backend Service Configuration
# ===========================================

# Analytics Service URL (internal, for URL Service to call)
ANALYTICS_SERVICE_URL=http://analytics-service:8081
